// Generated by acss-app-builder /app-layout sidebar
// {{IMPORT_SOURCE:}}
import type { ReactNode } from 'react'
import './AppShell.scss'

export type AppShellProps = {
  header?: ReactNode
  nav?: ReactNode
  footer?: ReactNode
  children: ReactNode
}

export default function AppShell({ header, nav, footer, children }: AppShellProps) {
  return (
    <div className="app-shell" data-shell="sidebar">
      {nav && <nav className="app-shell__nav" aria-label="Primary">{nav}</nav>}
      {header && <header className="app-shell__header">{header}</header>}
      <main className="app-shell__main" id="main">
        {children}
      </main>
      {footer && <footer className="app-shell__footer">{footer}</footer>}
    </div>
  )
}
